import 'package:flutter/material.dart';

class SearchAndDiscoveryView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Third Page"),
    );
  }
}